---
type: Example
title: exception — API ปฏิเสธคำขอทั้งหมด — **"วันที่ปิดบัญชีต้องไม่ก่อนวันครบกำ
description: API ปฏิเสธคำขอทั้งหมด — **"วันที่ปิดบัญชีต้องไม่ก่อนวันครบกำหนดงวดล่าสุดที่ชำระแล้ว (15 มิ.ย.)"** · ไม่คำนวณและไม่คืนยอดปิดบัญชีใดๆ ออกมา · **การบังคับเกิดที่ฝั่ง API ตาม BR-miniloan-025@v1 ไม่ใช่แค่ปุ่มบนหน้าจอที่เลือกวันย้อนหลังไม่ได้อยู่แล้ว** — ตรงกับขอบที่ `CALC-miniloan-002@v1` ระบุไว้ว่าจำนวนวันติดลบ "เกิดขึ้นไม่ได้ในเส้นทางจริง" เพราะมีการกันไว้จริง ไม่ใช่แค่สมมติว่าไม่เกิด
resource: ../rules/BR-miniloan-022@v1.md
tags: [miniloan, example, exception]
id: EX-miniloan-151
status: draft
kind: exception
proves: [BR-miniloan-022@v1]
has_ui: true
timestamp: 2026-08-24T09:30:00+07:00
spec_hash: sha256:ecb749a496cdc652110dcd59f5e1026ba74e529c57733a535fdd60126f0d1718
---

# EX-miniloan-151

## กำหนดให้ (given)
บัญชีเดียวกับ EX-miniloan-074 (สถานะ Active · ชำระงวดที่ 5 ครบกำหนดและชำระแล้ววันที่ 15 มิถุนายน) — ผู้ใช้ที่พยายามข้ามการตรวจของหน้าจอ

## เมื่อ (when)
เรียก API ขอยอดปิดบัญชีก่อนกำหนดโดยระบุวันที่ปิด = 10 มิถุนายน — **ก่อน**วันครบกำหนดงวดล่าสุดที่ชำระแล้ว (15 มิถุนายน) จำนวนวันจึงติดลบ โดยไม่ผ่านหน้าจอ

## แล้ว (then)
API ปฏิเสธคำขอทั้งหมด — **"วันที่ปิดบัญชีต้องไม่ก่อนวันครบกำหนดงวดล่าสุดที่ชำระแล้ว (15 มิ.ย.)"** · ไม่คำนวณและไม่คืนยอดปิดบัญชีใดๆ ออกมา · **การบังคับเกิดที่ฝั่ง API ตาม BR-miniloan-025@v1 ไม่ใช่แค่ปุ่มบนหน้าจอที่เลือกวันย้อนหลังไม่ได้อยู่แล้ว** — ตรงกับขอบที่ `CALC-miniloan-002@v1` ระบุไว้ว่าจำนวนวันติดลบ "เกิดขึ้นไม่ได้ในเส้นทางจริง" เพราะมีการกันไว้จริง ไม่ใช่แค่สมมติว่าไม่เกิด

## พิสูจน์กฎ

- [BR-miniloan-022@v1](../rules/BR-miniloan-022@v1.md) ✅ ปัจจุบัน — ยอดปิดบัญชีก่อนกำหนด = เงินต้นคงเหลือ + ดอกเบี้ยค้างจ่ายถึงวันที่ปิด + ค่าธรรมเนียมปิดก่อนกำหนด 1% ของเงินต้นคงเหลือ · ไม่คิดดอกเบี้ยของงวดในอนาคตที่ยังไม่ถึงกำหนด · ดอกเบี้ยค้างจ่ายใช้ฐานนับวัน actual/365 (นับวันจริง หารด้วย 365) นับจากวันครบกำหนดงวดล่าสุดที่ชำระแล้วถึงวันที่ปิด — ปิดตรงวันครบกำหนดที่เพิ่งชำระ ดอกเบี้ยค้างจ่ายเป็น 0
