---
type: Example
title: exception — API ปฏิเสธด้วยเหตุผลเดียวกัน — **การบังคับเกิดที่ฝั่ง API ตา
description: API ปฏิเสธด้วยเหตุผลเดียวกัน — **การบังคับเกิดที่ฝั่ง API ตาม BR-miniloan-025@v1 ไม่ใช่แค่ปุ่มบนหน้าจอที่กดไม่ได้** · สถานะบัญชีและตารางผ่อนไม่ถูกแตะต้อง
resource: ../rules/BR-miniloan-052@v1.md
tags: [miniloan, example, exception]
id: EX-miniloan-119
status: draft
kind: exception
proves: [BR-miniloan-052@v1, BR-miniloan-052@v2]
has_ui: true
timestamp: 2026-08-24T09:30:00+07:00
spec_hash: sha256:06fd7f909c8e99050d955e382a0eb58da538df8e623a4fd930eccf59bfb450fe
---

# EX-miniloan-119

## กำหนดให้ (given)
บัญชีเดียวกัน และผู้ใช้ที่พยายามข้ามการตรวจของหน้าจอ

## เมื่อ (when)
เรียก API บันทึกการชำระด้วยยอดที่ทำให้ส่วนเกินมากกว่ายอดปิดบัญชี โดยไม่ผ่านหน้าจอ

## แล้ว (then)
API ปฏิเสธด้วยเหตุผลเดียวกัน — **การบังคับเกิดที่ฝั่ง API ตาม BR-miniloan-025@v1 ไม่ใช่แค่ปุ่มบนหน้าจอที่กดไม่ได้** · สถานะบัญชีและตารางผ่อนไม่ถูกแตะต้อง

## พิสูจน์กฎ

- [BR-miniloan-052@v1](../rules/BR-miniloan-052@v1.md) ❄️ — ถ้ายอดส่วนที่เกินมากกว่ายอดปิดบัญชีตาม BR-miniloan-022@v1 ระบบต้อง**ปฏิเสธการบันทึกทั้งรายการ** — ไม่บันทึกการชำระ ไม่ปิดงวด ไม่ตัดเงินต้น ไม่ออกตารางผ่อนฉบับใหม่ และไม่ปิดบัญชี · ต้องแจ้ง Operations พร้อมยอดปิดบัญชีที่ถูกต้อง เพื่อบันทึกใหม่ด้วยยอดที่ทำให้ส่วนเกินเท่ากับยอดปิดบัญชีพอดี · **ไม่มีการทอนคืนและไม่มียอดค้างของผู้สมัครเก็บไว้ในระบบ** — เงินส่วนที่จ่ายเกินไปจริงถูกจัดการนอกระบบ ตามกรอบที่ตกลงไว้ว่าระบบบันทึกผลของการชำระที่เกิดขึ้นข้างนอกไปแล้ว ไม่ใช่ระบบรับเงินเอง · ผลที่ยอมรับแล้วคือระเบียนในระบบจะไม่ตรงกับเงินที่เคลื่อนจริงในกรณีนี้ จนกว่า Operations จะบันทึกใหม่ให้พอดี · ข้อจำกัดนี้ต้องบังคับที่ฝั่ง API ตาม BR-miniloan-025@v1 ไม่ใช่แค่กันที่หน้าจอ
- [BR-miniloan-052@v2](../rules/BR-miniloan-052@v2.md) ✅ ปัจจุบัน — ถ้ายอดส่วนที่เกินมากกว่ายอดปิดบัญชีตาม BR-miniloan-022@v1 **หรือ** ส่วนเกินหลังหักค่าธรรมเนียมการโปะ 1% ตาม BR-miniloan-050@v2 แล้วมากกว่าเงินต้นคงเหลือ ระบบต้อง**ปฏิเสธการบันทึกทั้งรายการ** — ไม่บันทึกการชำระ ไม่ปิดงวด ไม่ตัดเงินต้น ไม่ออกตารางผ่อนฉบับใหม่ และไม่ปิดบัญชี · ทั้งสองทริกเกอร์ให้ผลเดียวกัน จึงไม่แยกเป็นกฎคนละข้อ · ต้องแจ้ง Operations พร้อมยอดที่ถูกต้อง (ยอดปิดบัญชี หรือเงินต้นคงเหลือ แล้วแต่ทริกเกอร์ใดเข้าเงื่อนไข) เพื่อบันทึกใหม่ด้วยยอดที่ทำให้ส่วนเกินไม่ล้นอีก · **ไม่มีการทอนคืนและไม่มียอดค้างของผู้สมัครเก็บไว้ในระบบ** — เงินส่วนที่จ่ายเกินไปจริงถูกจัดการนอกระบบ ตามกรอบที่ตกลงไว้ว่าระบบบันทึกผลของการชำระที่เกิดขึ้นข้างนอกไปแล้ว ไม่ใช่ระบบรับเงินเอง · ผลที่ยอมรับแล้วคือระเบียนในระบบจะไม่ตรงกับเงินที่เคลื่อนจริงในกรณีนี้ จนกว่า Operations จะบันทึกใหม่ให้พอดี · ข้อจำกัดนี้ต้องบังคับที่ฝั่ง API ตาม BR-miniloan-025@v1 ไม่ใช่แค่กันที่หน้าจอ
