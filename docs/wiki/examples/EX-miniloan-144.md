---
type: Example
title: boundary — มี **หนึ่งรายการเท่านั้น** · การกันซ้ำต้องเกิดจาก **ข้อจำกัด
description: มี **หนึ่งรายการเท่านั้น** · การกันซ้ำต้องเกิดจาก **ข้อจำกัดไม่ซ้ำที่ฐานข้อมูล** ไม่ใช่จากการปิดปุ่มบนหน้าจอหรือการเช็คในหน่วยความจำของแอป · **ฟิลด์ที่ใช้เป็น key กันซ้ำของแต่ละคำสั่งยังไม่ถูกกำหนด (DQ-miniloan-009)** ใบนี้จึงบังคับที่ผลลัพธ์ ไม่ใช่ที่วิธี
resource: ../rules/BR-miniloan-043@v1.md
tags: [miniloan, example, boundary]
id: EX-miniloan-144
status: draft
kind: boundary
proves: [BR-miniloan-043@v1]
has_ui: true
timestamp: 2026-09-01T19:00:00+07:00
spec_hash: sha256:dd97cfca53b6c2010beccba3df8fad5534b258e537855d723dfd4bf239fff073
---

# EX-miniloan-144

## กำหนดให้ (given)
ฐานข้อมูลหลังจากการยิงซ้ำในใบก่อนหน้า — ขอบที่ตรวจคือ **ผลที่ตกค้างในฐานข้อมูล ไม่ใช่สิ่งที่หน้าจอแสดง**

## เมื่อ (when)
ตรวจจำนวนรายการใบสมัครที่เกิดขึ้นจริงจากการยิงสองครั้ง

## แล้ว (then)
มี **หนึ่งรายการเท่านั้น** · การกันซ้ำต้องเกิดจาก **ข้อจำกัดไม่ซ้ำที่ฐานข้อมูล** ไม่ใช่จากการปิดปุ่มบนหน้าจอหรือการเช็คในหน่วยความจำของแอป · **ฟิลด์ที่ใช้เป็น key กันซ้ำของแต่ละคำสั่งยังไม่ถูกกำหนด (DQ-miniloan-009)** ใบนี้จึงบังคับที่ผลลัพธ์ ไม่ใช่ที่วิธี

## พิสูจน์กฎ

- [BR-miniloan-043@v1](../rules/BR-miniloan-043@v1.md) ✅ ปัจจุบัน — ทุกคำสั่งที่เขียนข้อมูล (สร้างรายการใหม่ หรือเปลี่ยนสถานะ) ต้องกันการยิงซ้ำด้วยข้อจำกัดไม่ซ้ำ (unique constraint) ที่ฐานข้อมูล — คำสั่งซ้ำต้องถูกปฏิเสธพร้อม error ที่ผู้ใช้เห็น ไม่ใช่คืนผลของครั้งแรกเงียบๆ และไม่ใช่ปล่อยให้เกิดรายการซ้ำแล้วให้ผู้ใช้ไปลบเอง · ฟิลด์ที่ใช้เป็น key กันซ้ำของแต่ละคำสั่งยังไม่ถูกกำหนด (ดู DQ-miniloan-009)
