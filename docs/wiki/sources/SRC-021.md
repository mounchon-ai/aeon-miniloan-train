---
type: Source
title: chat SRC-021
description: บันทึกรอบเก็บสัญญาการคำนวณของ BR-miniloan-003@v1 (2026-08-22) — เจ้าของสเปกตอบผ่าน AskUserQuestion
รอบนี้ยิงจาก /req:cal
tags: [miniloan, source, chat]
id: SRC-021
kind: chat
captured_at: 2026-08-22T10:00+07:00
cited_by: [CALC-miniloan-005@v1]
timestamp: 2026-08-24T09:30:00+07:00
spec_hash: sha256:b9c14f78112aa9adbb052bb13897af3ce05139347b715a7e8bbf3232d049737c
---

# SRC-021

## ต้นฉบับ

| เรื่อง | ค่า |
|---|---|
| ชนิด | chat |
| ไฟล์ | — (แชท ไม่มีไฟล์) |
| เก็บเมื่อ | 2026-08-22T10:00+07:00 |
| เก็บด้วย | `/req:calc BR-miniloan-003@v1` |
| hash ตอน import | `—…` |

## เนื้อความ

> บันทึกรอบเก็บสัญญาการคำนวณของ BR-miniloan-003@v1 (2026-08-22) — เจ้าของสเปกตอบผ่าน AskUserQuestion รอบนี้ยิงจาก /req:calc BR-miniloan-003@v1 ถามทีละช่องตามลำดับเจ็ดช่องของสัญญา ไม่ได้ยิงจากคลังคำถาม ตรวจก่อนถาม (ทั้งสองทาง): BR-miniloan-003@v1 ไม่มีฟิลด์ constrained_by (เท่ากับ null) · calculations[].constrains ไม่มีตัวไหนชี้มาที่กฎเวอร์ชันนี้ → ไม่มีสัญญาเดิมให้ชน เขียนใหม่ได้โดยไม่ต้องผ่าน /req:change [1] formula · สูตรที่จะพินลงสัญญาเขียนแบบไหน       ⭐ a) MIN(5×รายได้ต่อเดือน, 1,000,000) — ค่าคงที่ (5 และ 1,000,000) ฝังในสูตรของสัญญาเอง ไม่ใช่ input เพราะเปลี่ยนต้องขึ้นเวอร์ชันกฎผ่าน /req:change ไม่ใช่ข้อมูลหลักที่มีเวอร์ชันแยกเหมือนอัตราดอกเบี้ย (BR-036/037)       b) ตัวคูณ/เพดานเป็นข้อมูลหลักแยกต่างหาก       z) ยังไม่แน่ใจ       → ตอบ a (ตามดาว) [2] inputs[] · สัญญานี้รับอินพุตอะไรบ้าง       ⭐ a) รายได้ต่อเดือนอย่างเดียว — inputs[] = monthly_income: money(2) · 5 กับ 1,000,000 เป็นค่าอธิบายในสูตร ไม่ใช่ input       b) เพิ่ม multiplier กับ cap เข้า inputs[] ด้วย       z) ยังไม่แน่ใจ       → ตอบ a (ตามดาว) [3] numeric_type + rounding_mode · 5×money(2) เป็นจำนวนเต็มสตางค์เสมอ (5×จำนวนเต็ม = จำนวนเต็ม ไม่มีเศษ) จำเป็นต้องมีจุดปัดจริงไหม       ⭐ a) decimal / HALF_UP ตาม BR-miniloan-035@v1 ที่ใช้ทั้งระบบ แต่ระบุไว้ตรงๆ ว่าสูตรนี้ไม่มีจุดปัดที่เกิดขึ้นจริง (5×money(2) และ MIN() กับค่าคงที่จำนวนเต็มบาทไม่สร้างทศนิยมใหม่)       b) integer (คิดเป็นหน่วยสตางค์ล้วนแทน decimal บาท)       z) ยังไม่แน่ใจ       → ตอบ a (ตามดาว) [4] rounding_points · ตามข้อ [3] — ไม่มีจุดปัดจริง เขียนสัญญาให้สะท้อนตรงนี้อย่างไร       ⭐ a) ระบุชัดว่า "ไม่มีจุดปัดที่เกิดขึ้นจริง" แทนที่จะประดิษฐ์จุดปัดขึ้นมาเติมฟิลด์ — 5 เป็นจำนวนเต็ม, 1,000,000 เป็นจำนวนเต็มบาท, MIN() ไม่แนะนำทศนิยมใหม่ ดังนั้นผลลัพธ์ยังอยู่ที่ 2 ตำแหน่งเท่าที่ money(2) ของ monthly_income มีอยู่แล้ว       b) อื่น       z) ยังไม่แน่ใจ       → ตอบ a (ตามดาว) [5] residual_policy · สูตร MIN(5×รายได้, 1,000,000) ไม่มีการแบ่งงวดหรือเศษใดๆ       ⭐ a) เว้นเป็น null — ไม่มีเศษให้ต้องมีนโยบาย       b) มีนโยบายอื่น       z) ยังไม่แน่ใจ       → ตอบ a : residual_policy = null [6] boundary_behavior[] · ขอบที่ต้องเขียนไว้ในสัญญา       ⭐ a) สามขอบ: (1) รายได้×5 < 1,000,000 → ใช้สูตร 5× ตรงๆ ตาม EX-miniloan-019/020 · (2) รายได้×5 = 1,000,000 พอดี (รายได้ 200,000) → สองสูตรให้คำตอบเดียวกันพอดี ไม่มีนัยว่าใช้ทางไหน ตาม EX-miniloan-021 · (3) รายได้×5 > 1,000,000 → ถูกจำกัดที่เพดาน ตาม EX-miniloan-022 — รายได้ขั้นต่ำ 15,000 จาก BR-miniloan-001@v1 ไม่นับเป็นขอบของสัญญานี้ เพราะเป็นเคสปกติที่ไม่ชนขอบอะไรเลย (75,000 << 1,000,000)       b) เพิ่ม/แก้ขอบ       z) ยังไม่แน่ใจ       → ตอบ a (ตามดาว) [7] deferred_questions · decimal(p,s) ของเงิน (รายได้/วงเงิน) ค้างที่ DQ-miniloan-001 เหมือนกันกับ CALC-001/002/003/004 หรือคนละเรื่อง       ⭐ a) ใช้ DQ-miniloan-001 ซ้ำ — เป็นคำถามระดับ Money ทั้งระบบเหมือนกัน ไม่ใช่คำถามใหม่แยกต่างหาก       b) เปิด DQ ใหม่แยก       z) ยังไม่แน่ใจ       → ตอบ a (ตามดาว) สรุปการเปิดการ์ด: ไม่มีช่องไหนใน [1]–[7] ตอบ "ยังไม่แน่ใจ" → รอบนี้ไม่เปิดการ์ดแดงใหม่ DQ ที่ผูกเข้าสัญญา: DQ-miniloan-001 (decimal(p,s) และจำนวนตำแหน่งที่ปัด) — เปิดค้างอยู่ ปิดที่นี่ไม่ได้ ใช้ร่วมกับ CALC-miniloan-001/002/003/004 เพราะเป็นคำถามระดับระบบเดียวกัน ไม่ใช่คำถามใหม่แยกต่างหาก

## ใครอ้างถึง

- [CALC-miniloan-005@v1](../calculations/CALC-miniloan-005@v1.md)
