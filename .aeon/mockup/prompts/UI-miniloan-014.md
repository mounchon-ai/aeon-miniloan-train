# UI-miniloan-014 · พิจารณาคำขอปรับปรุงบัญชีที่ปิดแล้ว

> ไฟล์นี้ generate จาก wireframe — **ห้ามแก้ด้วยมือ** แก้ที่ต้นทางแล้วสร้างใหม่ (ประตู 23)
> วางข้อความทั้งไฟล์นี้ให้เครื่องมือออกแบบ แล้วเอาผลลัพธ์กลับมาที่ `<state-dir>/mockup/handoff/<slug>/`

## 1 · บริบทของหน้า

- **หน้าจอ:** `UI-miniloan-014`
- **จุดประสงค์:** ผู้ถือ role ผู้อนุมัติเปิดดูคำขอ เทียบค่าเดิมกับค่าใหม่ แล้วอนุมัติหรือปฏิเสธ
- **ผู้ใช้ที่เห็นหน้านี้:** `ROLE-005`
- **ชนิดของหน้า:** `rows` · **ระดับที่มีอยู่แล้ว:** `L1`

## 2 · สิ่งที่ต้องมี

### โซน `header` (header)

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| title | พิจารณาคำขอปรับปรุงบัญชีที่ปิดแล้ว | — | — ยังไม่มีใครตั้งชื่อ |

### โซน `adjustment-detail` (columns)

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | ชื่อฟิลด์ที่ขอแก้ | — | `ui-miniloan-014-ent-010-field-name` |
| field | ค่าเดิม | — | `ui-miniloan-014-ent-010-old-value` |
| field | ค่าใหม่ | — | `ui-miniloan-014-ent-010-new-value` |
| field | ผู้ยื่นคำขอ | — | `ui-miniloan-014-ent-010-requested-by` |
| field | วันที่ยื่นคำขอ | — | `ui-miniloan-014-ent-010-requested-at` |
| action | อนุมัติคำขอ | → กลับไปที่รายการคำขอปรับปรุงบัญชีที่รออนุมัติ | `ui-miniloan-014-approve-adjustment` |
| action | ปฏิเสธคำขอ | → กลับไปที่รายการคำขอปรับปรุงบัญชีที่รออนุมัติ | `ui-miniloan-014-reject-adjustment` |

**ทุก `data-testid` ข้างล่างนี้ต้องกลับมาครบในหน้าที่ส่งกลับ** — id พวกนี้เปลี่ยนไม่ได้หลังเกิด (ประตู 37) และ `qa` ใช้มันเขียนเทส

- `ui-miniloan-014-approve-adjustment`
- `ui-miniloan-014-ent-010-field-name`
- `ui-miniloan-014-ent-010-new-value`
- `ui-miniloan-014-ent-010-old-value`
- `ui-miniloan-014-ent-010-requested-at`
- `ui-miniloan-014-ent-010-requested-by`
- `ui-miniloan-014-reject-adjustment`

## 3 · ห้าสถานะ

| สถานะ | เกิดกับหน้านี้ไหม | ข้อความที่ต้องแสดง |
|---|---|---|
| `empty` | ไม่เกิด | ไม่เกิด — เข้าหน้านี้ได้ต่อเมื่อมีคำขอปรับปรุงอยู่แล้วเท่านั้น |
| `loading` | เกิด | ระหว่างกดอนุมัติหรือปฏิเสธ ปุ่มที่กดถูก disable และแสดง spinner |
| `error` | เกิด | แสดงข้อความปฏิเสธการอนุมัติ เมื่อผู้ยื่นคำขอพยายามอนุมัติของตัวเองตามหลัก four-eyes ตาม BR-miniloan-049@v1 |
| `unauthorized` | เกิด | แสดงข้อความว่าไม่มีสิทธิ์พิจารณาคำขอนี้ เมื่อผู้ใช้ที่ไม่ได้ถือ role ผู้อนุมัติพยายามเข้าถึง |
| `overflow` | ไม่เกิด | ไม่เกิด — เนื้อหาต่อหนึ่งคำขอมีจำนวนคงที่ |

_ข้อความในตารางนี้คัดมาคำต่อคำจากที่ design ประกาศไว้ **ห้ามเขียนใหม่ให้สวยขึ้น**_

## 4 · กติกา UI ที่บังคับ

- **UIC-001** — ปุ่มประจำแถวอยู่คอลัมน์แรกของแถว เรียงลำดับตายตัว: view · edit · delete · manage · command
  - ใช้กับ: ทุกหน้าที่แสดงเป็นรายการแถว — ปุ่มประจำแถวทุกปุ่ม (ชนิด view · edit · delete · manage · command — ชนิดเป็นของ design ตั้งแต่ประตู 35)
- **UIC-002** — แสดงเป็นไอคอน ไม่ใช่ปุ่มตัวหนังสือ และใช้ชื่อ action ที่ design ประกาศไว้ใน actions[].name เป็นชื่อสำหรับ screen reader
  - ใช้กับ: ปุ่มประจำแถวทุกปุ่ม
- **UIC-003** — เปิดเป็น modal ทับรายการ ไม่พาไปหน้ารายละเอียดแยก
  - ใช้กับ: ปุ่มประจำแถวชนิด view

