/** Surface container for grouping content — application summaries, schedule panels, KPI tiles. */
export interface CardProps {
  children: React.ReactNode;
  padding?: number;
  style?: React.CSSProperties;
}
