# UI-miniloan-016 · บันทึกการชำระ

> ไฟล์นี้ generate จาก `wireframes/UI-miniloan-016.json` — **ห้ามแก้ด้วยมือ** แก้ที่ต้นทางแล้วสร้างใหม่ด้วย `/mock:wireframe` (ประตู 23)
> ใบนี้คือ **wireframe L1 ที่คนอ่าน** — โครงของหน้า ไม่มีสี ไม่มีขนาด · ภาพที่มีธีมอยู่ที่ `/mock:html` (L2) · ใบคำขอถึงเครื่องมือออกแบบอยู่ที่ `/mock:handoff --prompt`

## 1 · บริบทของหน้า

- **หน้าจอ:** `UI-miniloan-016`
- **จุดประสงค์:** ให้ Operations บันทึกยอดที่รับชำระเข้าบัญชีหนึ่งบัญชี ระบบจัดว่าเป็นการชำระตรงงวดหรือการโปะเงินต้นเองตามยอดที่กรอก
- **ผู้ใช้ที่เห็นหน้านี้:** `ROLE-004`
- **ชนิดของหน้า:** `form` · **ระดับที่มีอยู่แล้ว:** `L1`
- **ภาพนี้:** `MCK-miniloan-016` · ชนิดของหน้าที่ design ประกาศ: `edit` · ไฟล์ต้นทาง: `wireframes/UI-miniloan-016.json`

## 2 · สิ่งที่ต้องมี

_โซนเรียงตามลำดับที่หน้าถูกวาด · `วางโดย` บอกว่าใครเป็นคนจัดโซนนี้ — `design` คือผู้ประกาศ `zones[]` (ประตู 47) ส่วน `mock` คือโซนที่ตัวเรนเดอร์เพิ่มให้เอง_

### โซน `header` (header) — วางโดย `mock`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| title | บันทึกการชำระ | — | — ยังไม่มีใครตั้งชื่อ |

### โซน `account` (form) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | เงินต้นคงเหลือ | — | `ui-miniloan-016-ent-007-outstanding-principal` |
| field | งวดที่ค้างชำระ | — | `ui-miniloan-016-ent-009-installment-number` |
| field | ยอดงวดที่ต้องชำระ | — | `ui-miniloan-016-ent-009-emi-amount` |

### โซน `payment` (form) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | ยอดที่รับชำระ | — | `ui-miniloan-016-ent-010-amount` |
| action | บันทึกการชำระ | → อยู่ที่หน้าเดิม | `ui-miniloan-016-record-payment` |

## 3 · ห้าสถานะ

| สถานะ | เกิดกับหน้านี้ไหม | ข้อความที่ต้องแสดง |
|---|---|---|
| `empty` | ไม่เกิด | ไม่เกิด — เข้าถึงจากรายการบัญชีที่ฉันดูแลเท่านั้น ซึ่งมีงวดค้างอยู่เสมอที่ทำให้เปิดหน้านี้ได้ |
| `loading` | เกิด | แสดง skeleton/spinner ทับตำแหน่งข้อมูล ปุ่มที่ยิง action ถูกปิดใช้งานชั่วคราวระหว่างรอ |
| `error` | เกิด | แสดงข้อความ "เกิดข้อผิดพลาด ลองใหม่อีกครั้ง" พร้อมปุ่มลองใหม่ ข้อมูลเดิมที่เคยโหลดสำเร็จยังคงอยู่บนจอ ไม่หายไป ยอดที่กรอกไว้ไม่หาย |
| `unauthorized` | เกิด | แสดงข้อความ "บัญชีนี้ไม่ได้ถูก assign ให้คุณดูแล" แทนข้อมูล |
| `overflow` | ไม่เกิด | ไม่เกิด — บันทึกทีละหนึ่งรายการชำระ |

_ข้อความในตารางนี้คัดมาคำต่อคำจากที่ design ประกาศไว้ **ห้ามเขียนใหม่ให้สวยขึ้น**_

## 4 · กติกา UI ที่บังคับ

- **UIC-001** — ปุ่มประจำแถวอยู่คอลัมน์แรกของแถว เรียงลำดับตายตัว: view · edit · delete · manage · command
  - ใช้กับ: ทุกหน้าที่แสดงเป็นรายการแถว — ปุ่มประจำแถวทุกปุ่ม (ชนิด view · edit · delete · manage · command — ชนิดเป็นของ design ตั้งแต่ประตู 35)
- **UIC-002** — แสดงเป็นไอคอน ไม่ใช่ปุ่มตัวหนังสือ และใช้ชื่อ action ที่ design ประกาศไว้ใน actions[].name เป็นชื่อสำหรับ screen reader
  - ใช้กับ: ปุ่มประจำแถวทุกปุ่ม
- **UIC-003** — เปิดเป็น modal ทับรายการ ไม่พาไปหน้ารายละเอียดแยก
  - ใช้กับ: ปุ่มประจำแถวชนิด view

